#! /usr/bin/env bash
cd ~/assesment_week_1/material-design-template/
echo "pulling from remote repo..."
#change branch if we are already in other one
git checkout main
#Getting from remote repository
git pull origin
